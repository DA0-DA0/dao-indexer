version: "3.9"

services:
  db:
    container_name: indexer_db
    image: postgres:14
    volumes:
      - ./data/db:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - '5432:5432'
  indexer:
    build: .
    command: bash -c "cargo install diesel_cli --no-default-features --features "postgres" && diesel setup && diesel migration run && cargo run" 
    # diesel migration run && cargo run
    #  bash -c "diesel setup && diesel migration run && cargo watch -x run"
    volumes:
      - .:/code
    network_mode: host 
    environment:
      - POSTGRES_NAME=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - ENABLE_INDEXER=true
      - TENDERMINT_RPC_URL=13.57.136.13:26656
      # - TENDERMINT_WEBSOCKET_URL=ws:13.57.136.13:26656/websocket
      # ws://127.0.0.1:26657/websocket
      - TENDERMINT_WEBSOCKET_URL=wss://rpc.uni.juno.deuslabs.fi/websocket
      - TENDERMINT_RPC_URL=https://rpc.uni.juno.deuslabs.fi
      - TENDERMINT_INITAL_BLOCK_HEIGHT=100000
      - RUST_BACKTRACE=1
      - DATABASE_URL=postgres://postgres:postgres@localhost:5432/postgres
      #- DATABASE_URL=postgres://postgres:postgres@indexer_db:5432/postgres
    depends_on:
      - db
